import { CounterReducer, State, StateUser } from ".."

const Hooks = () => {

    return (
        <div>

            <State />

            <StateUser />

            <CounterReducer />

        </div>
    )
}

export default Hooks