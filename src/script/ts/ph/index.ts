// eslint-disable-next-line @typescript-eslint/no-inferrable-types
const user: string = 'taiseen azam';
console.log(user);

// tuple data type/structure...
const person: [string, number] = ['taiseen', 30];
console.log(person)

// tuple is just a fixed length [array]
// where (each item) in that [array] has its own type...